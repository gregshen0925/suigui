import { useQuery } from "@tanstack/react-query";
// import { createNewKey } from "../utils/createNewKey";

// export function useSui() {
//   const { data: newKey } = useQuery({
//     queryKey: ["createNewKey"],
//     queryFn: () => createNewKey(),
//   });

//   return { newKey };
// }
